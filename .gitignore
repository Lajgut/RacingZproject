# AIM Race Studio raw binary files — too large for git, useless without Race Studio
# Store locally; commit only .csv/.xlsx exports
*.xrk
*.xrz
*.rrk
*.gpk
*.drk
*.bak

# Original zip archives
*.zip

# Сырые телеметрийные CSV (тяжёлые, 8+ МБ каждый) — только локально.
# Анализ (скрипт + отчёт) коммитим, сырые данные — нет.
engineering/data/telemetry/**/*.csv

# macOS
.DS_Store
